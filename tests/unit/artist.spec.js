/* eslint-disable no-console */
/* eslint-disable no-undef */
import Artist from '../../src/components/artist.vue';

describe('artist.vue', () => {
  test('sanity test', () => {
    console.log(Artist);
  });
});
